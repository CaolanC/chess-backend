{
  "name": "chessplusplus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "npm install && git submodule init && git submodule update && npm run init --prefix ./front/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "ts-node ./src/index.ts",
    "run": "npm run build && npm run start",
    "random_secret": "node ./dev-scripts/generate_secret_token.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.7.5",
    "ts-node-dev": "^2.0.0"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "typescript": "^5.6.3",
    "uuid": "^10.0.0"
  }
}
